# Lime: план развития конструктора с низким входом и высоким потолком

Дата: 2026-07-01 · Ревизия: 2026-07-04 (Milestones 0-3 закрыты, M4 закрыт частично — клиентский срез, M5 Фазы A-D закрыты: реальный embed-эталон + publish + перф-замер; формат Experience Pack + canvas-first вход; уровни UI Basic/Design/Motion/Pro; готовые рецепты сцен + человеческие пресеты; требования к asset slot + честная пометка про видео; AI заполняет текст пака по брифу; AI подбирает тему+motion под настроение; AI предлагает промпт-бриф под asset slot; pack-level mobile adapter; второй Experience Pack — Studio Folio, портфолио/creative studio)

## Summary

Цель: развить Lime так, чтобы новичок начинал с простого холста и готовых experience packs, а продвинутый пользователь постепенно доходил до уровня интерактивных брендовых сайтов вроде ChainZoku: сильные ассеты, сцены, слои, motion, 3D/embed, компоненты и кастомизация.

Первый фокус: curated showcase-вертикаль для креаторов, Web3, игр и брендов, вдохновленная уровнем ChainZoku, но без копирования его ассетов, текста или айдентики. Результат первого этапа: демо + reusable-инструменты, production-ready по адаптиву, безопасности и скорости.

## Реальность и границы (честная рамка)

Этот раздел важнее остального документа: без него план превращается в обещание, которое движок не сдержит.

- ChainZoku — это НЕ вывод конструктора. Это bespoke creative-dev работа студии: кастомный Three.js/WebGL, собственные GLSL-шейдеры, GSAP ScrollTrigger с посекционным пиннингом и синхронизацией камеры со скроллом, кастомный курсор, прелоадер, звук, сквозная арт-дирекция (каждая секция уникальна, а не «сетка карточек»). Такое пишется руками неделями под конкретный проект. Ни Webflow, ни Framer, ни Wix Studio не генерируют это из шаблонов — на них делают близкое, но всегда руками дизайнера-разработчика поверх движка.
- Реалистичная цель Lime: не копия, а «сайт, который обычного посетителя впечатляет не меньше» — ~70–80% воспринимаемого «вау» через embed-first + сильные motion-рецепты + хорошие ассеты.
- Жёсткий потолок embed-подхода (принять как данность на первом этапе): embed живёт в отдельном iframe и НЕ синхронизируется со скроллом хост-страницы без кастомного кода, которого в конструкторе нет. Значит: бесшовной хореографии «камера+текст+переходы в одном тайминге», кастомных шейдеров, вплетённых в скролл страницы, и уникальной по-секционной арт-дирекции первый этап не даёт.
- Бутылочное горлышко — не движок, а ассеты и арт-дирекция. Результат на ~80% зависит от того, появится ли сильная Spline-сцена, кастомный 3D-персонаж, продуманная палитра и типографика. Движок — множитель качества ассетов, а не его источник. Строка из Assumptions «уровень ChainZoku = качество композиции и ассетов, а не копирование» — самая точная в документе; держимся за неё.

## Key Changes

- Добавить слой Experience Packs поверх текущих `LimeTemplates` и `LimePresets`: статические curated-паки без новой БД на первом этапе.
- Формат пака: `key`, `name`, `category`, `level`, `theme`, `sections`, `assetSlots`, `motionProfile`, `preview`.
- Первый пак: `neo-lore-drop`: hero, lore intro, factions/clans, vision cards, customization embed, team, FAQ, Discord/CTA, footer.
- ВНИМАНИЕ: часть секций пака — это НОВЫЕ типы, а не композиция. Текущие 14 пресетов (navbar, hero, logos, features, stats, steps, split, pricing, testimonials, team, faq, contact, cta, footer) НЕ содержат `lore intro`, `factions/clans`, `vision cards`, `customization embed`. Их надо спроектировать и добавить в `LimePresets` как новые пресеты — это отдельная работа, а не «расширить существующие». Переиспользуем: navbar, hero, team, faq, cta(→Discord), footer.
- Перестроить вход в редактор под canvas-first: пустой документ открывает холст и стартовую панель "Выбрать пак / начать с секции / AI-идея", без обязательного мастера.
- Существующий редактор остается единым, но получает уровни UI: `Basic`, `Design`, `Motion`, `Pro`.
- Сделать раскрытие функций обучающим, не paywall:
  - `Basic`: текст, картинки, секции, тема.
  - `Design`: layout, free-mode, reusable classes, компоненты.
  - `Motion`: сцены, parallax, marquee, reveal, декоративные слои.
  - `Pro`: custom CSS/head, export, embed/3D, advanced SEO.
- Новых тарифных гейтов не добавлять; существующие ограничения custom code оставить как есть.
- Усилить motion/scene UX поверх уже существующих `scene`, `parallax`, `marquee`, `layers`.
- Добавить готовые рецепты сцен: pinned hero, horizontal cards, layered parallax, reveal sequence, marquee strip.
- В инспекторе показывать человеческие пресеты вместо голых чисел там, где это возможно.
- Улучшить media/asset workflow: добавить asset slots для паков: logo, hero character, background, decor layer, video/poster, 3D/embed.
- Для каждого asset slot показывать требования: формат, прозрачность, рекомендуемые размеры, mobile-safe crop.
- AI-ассеты делать смешанно: Lime предлагает промпты, идеи или генерацию, но пользователь может загрузить свои медиа.
- 3D в первом этапе делать embed-first: улучшить текущий `embed` как управляемый блок для Spline/Rive/Lottie/iframe-сцен.
- Embed — точная делта (проверено 2026-07-03). УЖЕ сделано: sandbox (`allow-scripts allow-same-origin allow-popups`), `loading="lazy"`, https-only regex на вводе и рендере, host-allowlist, `provider`/`aspect`/`poster`/`fallbackTitle`/`fallbackText` в контенте, aspect ratio на wrapper, poster/fallback до загрузки iframe, provider-пресеты в `promptEmbed` (Spline/Rive/Lottie/YouTube/Vimeo/Sketchfab/Figma вместо голого URL-prompt).
- ✅ СДЕЛАНО 2026-07-04: реальный hero/customizer-embed подобран и замерен. Sketchfab-сцена «Cyberpunk City – #1» by John Doe (CC BY 4.0, коммерческое использование разрешено с атрибуцией — атрибуция выведена постоянной подписью под embed, не только в fallback) — `https://sketchfab.com/models/14d2eaa145ee42938e004115871adf6c/embed`, `provider:"sketchfab"`, постер — реальный JPEG-thumbnail с media.sketchfab.com (не placeholder). Проверено headless-браузером (не просто fetch — статический fetch embed-страницы обманчиво показывает служебный `noscript`-fallback-текст «too heavy», реальный рендер в Chromium подтвердил рабочую 3D-сцену).
- ✅ СДЕЛАНО 2026-07-03 (безопасность): host-allowlist для embed-URL — `LimeDoc.isAllowedEmbedUrl`/`EMBED_HOSTS` (spline.design, rive.app, lottie.host, lottiefiles, youtube, vimeo, sketchfab, figma; домен или поддомен, порт/user@-трюки отклоняются). Проверка в общем рендере → действует в редакторе, node-тестах и на publish (Jint) одинаково; плюс валидация на вводе в `promptEmbed` с человеческим сообщением. Произвольный https-iframe запрещён (пользователей ещё нет — ослабить можно позже). Тесты: 8 проверок в `tests/lime-doc.selftest.cjs`, dotnet 250/250 (паритет цел), editor-v2 41/41.
- Native Three.js/GLB viewer не входит в первый этап.
- Расширить AI из генератора базового лендинга в creative assistant — но фазами, а не одним прыжком. Каждый новый навык AI = новая командная поверхность со своей валидацией в allowlist `LimeCommands.validateAiCommands`; нельзя открывать всё сразу.
  - Фаза A: «заполнить секции пака текстом» (уже близко к текущему `setContent`/`aiSuggest`).
  - Фаза B: «выбрать experience pack + применить theme/motion-команды» (новые типы команд, расширить allowlist и dry-run).
  - Фаза C: «предложить asset prompts под слоты» (текстовый вывод-бриф, без генерации файлов).
  - Фаза D: «адаптировать mobile» (уже есть `aiAdaptMobile`, только довести под новые секции).
- Ответы AI по-прежнему только JSON/commands, без произвольного HTML; каждый новый тип команды проходит validate → dry-run на клоне → preview → один undo (инвариант текущего пайплайна не ослаблять).

## Implementation Notes

- Основной путь (актуализировано 2026-07-03, после разбивки god file 2625→1456 / 38 модулей): паки и шаблоны → `lime-editor-presets.js` — runtime-применение пака это буквально готовое API `LimeEditorPresets.applyTemplateByKey`/`insertPreset` (spec `blockFromSpec` поддерживает styles/css/anim/layers/scene/fx; stale-doc фикс под runtime-вызов сделан, регрессия покрыта `tests/lime-presets.selftest.cjs`); данные секций → `lime-presets.js`/`lime-templates.js`; motion-рецепты → `lime-editor-effects.js`; embed UI → `lime-editor-media-actions.js` (`promptEmbed`); панель/инспектор → `lime-editor-inspector.js`/`lime-editor-inspector-events.js`; запись стилей → `lime-editor-style-engine.js`; AI → `lime-editor-ai-pipeline.js` + `AiContentService`. В `lime-doc-editor.js` (композиционный корень) — только инъекции.
- Тесты новых модулей — по установленному паттерну node-селфтестов `tests/lime-*.selftest.cjs` (22 существующих как образец) + editor-v2 Playwright после каждого среза.
- Не ломать текущий JSON-документ: новые поля должны быть additive и безопасно игнорироваться старым рендером.
- Showcase pack должен использовать только свои, свободные или сгенерированные заглушки, не ChainZoku assets.
- Публикация должна оставаться безопасной: sandbox для embed, sanitize custom head, без editor-only атрибутов в publish.
- Asset-пайплайн (backend) — ПРОВЕРЕНО 2026-07-03. Что уже есть и достаточно: аплоад 5 MB с трёхслойной валидацией (расширение/MIME/magic-bytes: jpg/png/gif/webp), ImageSharp resize до 1920 по большей стороне, jpeg q82, decompression-bomb guard. Три именованные дыры до «уровня ChainZoku»:
  - ✅ (а) СДЕЛАНО 2026-07-03: `/media/**` отдаётся с `Cache-Control: public, max-age=31536000, immutable` (`Startup.UseStaticFiles.OnPrepareResponse`; имена — GUID, контент по URL неизменен). dotnet build/tests зелёные.
  - (б) видео загрузить НЕЛЬЗЯ вообще: видео-блок = только YouTube, фон-видео секции (`bg.videoSrc`/`poster`) рендерится, но принимает только внешний URL. Слот «video/poster» из asset slots требует либо видео-аплоада (mp4/webm, отдельный лимит и валидация), либо честной пометки «v1: видео только внешним URL».
  - (в) avif не принимается (webp есть) — опционально, не блокер.

## Milestones и секвенсинг

Порядок обязателен: сначала доказываем потолок одним ручным эталоном, только потом строим переиспользуемую инфраструктуру.

- Milestone 0 — эталонный спайк (1–2 недели, приоритет №1). Собрать ОДИН `neo-lore-drop` ВРУЧНУЮ на текущем движке, выжав максимум: настоящая Spline-сцена в hero, реальные ассеты, все motion-рецепты, mobile-fallback, publish без ошибок. Цель — не код на переиспользование, а честный ответ «дотягивает ли embed-first до того, что я представлял». Definition of done: опубликованная страница, на которую не стыдно смотреть, с зафиксированным перф-замером. Это gate: если результат не убеждает — паузим инфраструктуру и пересматриваем подход.
  - ✅ СТАТУС 2026-07-04: ЗАКРЫТ. `neo-lore-drop` опубликован на живом локальном стенде (sign-up → EditDoc?template=neo-lore-drop → save → Publish → `/u/<user>/<slug>`) и провёрен headless-Chromium под мобильным throttling (Pixel 5, GSAP-эквивалент Lighthouse mobile: Slow-4G 1.6/0.75 Mbps, RTT 150ms, CPU×4): **LCP ≈ 2.0–2.1с** (бюджет ≤2.5с — выполнен), **CLS ≈ 0.011** (well under 0.1), 0 JS-ошибок в консоли. LCP-элементом остаётся poster (не iframe) — лениво подгружаемый embed не бьёт LCP, как и задумано в перф-бюджете.
  - По ходу спайка найдены и исправлены 2 pre-existing бага в горизонтальной сцене (не связаны с embed-правкой, но блокировали DoD «не стыдно смотреть»): (1) `constructor.css` — `.lime-block__children--scene` не задавал `flex-direction:row`, из-за чего наследовался `column` от базового `.lime-block__children` и десктопный horizontal-scene рендерился вертикальным стеком без scroll-jack (GSAP ScrollTrigger не создавал pin, `dist<=0`); добавлен явный `flex-direction: row`. (2) `lime-presets.js` (`neo-factions`) — scoped CSS ставил `max-width:460px` на карточки сцены, что конфликтовало с движковым `flex:0 0 70vw` и не давало треку переполниться по ширине (тоже гасило pin); правило убрано. После обоих фиксов: `pinSpacers:1`, трек 3072px против viewport 1440px, карточки едут горизонтально при скролле — проверено скриншотами по шагам скролла. Оба фикса покрыты существующими node-селфтестами и dotnet-сьютом (250/250 зелёных), визуально проверены вручную (Playwright-скрипт, не сохранён в репо — одноразовый).
  - Перф-замер спайка: cache-headers для медиа закрыты 2026-07-03 (дыра «а») — замер честный, подтверждено выше.
- Milestone 1 — извлечь эталон в Experience Pack: вынести новые секции в `LimePresets`, оформить формат пака, canvas-first вход с панелью «Выбрать пак».
  - ✅ СТАТУС 2026-07-04: ЗАКРЫТ. `neo-*` секции уже были в `LimePresets` со времён спайка M0 — оставалось оформить формат пака и дать canvas-first вход. Формат: новый тонкий слой `lime-experience-packs.js` (`window.LimeExperiencePacks = { LIST, resolve }`) хранит ТОЛЬКО новые поля пака (`category`, `level`, `assetSlots`, `motionProfile`, `preview`), а `name`/`theme`/`sections` резолвятся из совпадающего `key` в `LimeTemplates` — без дублирования данных (DRY, как и в самих шаблонах). Пока один пак: `neo-lore-drop` (`category:"showcase"`, `level:"pro"`, 3 asset-слота, 3 motion-профиля). Canvas-first вход: существующий intro-оверлей пустого документа (`#lime-doc-intro`, AI-промпт-флоу не тронут и остаётся первичным) получил вторичную панель «или начни с готового пака» — клик по тайлу зовёт `applyTemplateByKey` мгновенно на холсте (без `?template=key` и перезагрузки страницы), тайл рендерится из `LimeExperiencePacks`. «Начать с секции» не потребовало новой UI — это уже существующие тайлы пресетов в сайдбаре, доступные сразу после dismiss интро.
  - Проверено: node-селфтесты (новый `tests/lime-experience-packs.selftest.cjs` + полный прогон всех `lime-*.selftest.cjs`) зелёные, dotnet 250/250 (бэкенд не тронут). E2E на живом стенде (Playwright, одноразовый скрипт): пустой `/Home/EditDoc` → интро показывается → тайл пака кликается → 58 блоков на холсте, интро прячется → save создаёт сайт. Регрессия: AI-chips/skip-кнопка на пустом документе по-прежнему на месте — существующий AI-first флоу не задет.
  - Вне рамок этого этапа (сознательно): UI редактирования asset-слотов (Milestone 4), уровни Basic/Design/Motion/Pro (Milestone 2), второй пак (формат просто поддерживает больше одного).
  - ✅ СТАТУС 2026-07-04 (2): ВТОРОЙ ПАК ЗАКРЫТ — `studio-folio` (портфолио/creative studio), по явному выбору
    пользователя из названных в Assumptions направлений (SMB/портфолио/ecommerce). Деликатно противоположная
    эстетика неон-киберпанку `neo-lore-drop`: тёплая светлая палитра (`accent:"#c4531f"`, `bg:"#f6f3ee"`),
    серифный Playfair Display поверх тела-шрифта `Work Sans` (оба — из курируемого списка `lime-fonts.js`,
    сверено с `PublishedPageBuilder.FontFamilies`, чтобы шрифт не отвалился на публикации). Доказывает, что
    формат пака не завязан на один визуальный язык — `LimeExperiencePacks.resolve()` и рендер тайлов в
    `lime-editor-intro.js` оказались уже полностью pack-count-agnostic (ноль правок, только новая запись в
    `LIST`+`LimeTemplates`). 10 новых пресетов `folio-*` в `lime-presets.js` (навбар/hero/работы/о нас/
    клиенты-marquee/рил/отзывы/faq/cta/подвал) + новый локальный хелпер `folioCard()`; переиспользован уже
    существовавший, но не задействованный в первом паке `marquee`-примитив (Milestone 3) для бегущей строки
    клиентов. 3 asset-слота (`hero-portrait`, `reel-embed`, `logo`) на русском (в отличие от английских слотов
    `neo-lore-drop` — не чинили ту непоследовательность, просто не повторили). Демо-рил — реально проверенное
    (headless-скриншот + прогон через настоящий http-origin, не `about:blank`) официальное CC BY видео Blender
    Foundation «Big Buck Bunny» с канала YouTube «Blender» (`aqz-KE-bpKQ`), не выдуманный ID.
  - Найдено при проверке (не баг продукта, а особенность превью редактора): переключатель брейкпоинта в
    редакторе сужает контейнер холста через `max-width`, но НЕ меняет реальный viewport браузера — поэтому
    CSS `@media`-запросы (например, коллапс общей `layout:"bento"`-сетки в 1 колонку на ≤640px) не срабатывают
    в этом симулированном превью, хотя корректно работают на настоящей опубликованной странице при реальном
    узком viewport (проверено — 0px переполнения). Не чинили (не баг), просто не полагались на превью-брейкпоинт
    как на источник истины для проверки адаптива — как и раньше, реальный чек только через настоящий Playwright-viewport на опубликованной странице.
  - Проверено: `tests/lime-experience-packs.selftest.cjs` (LIST.length===2, resolve(studio-folio), палитра
    отличается от neo-lore-drop, второй тайл рендерится без единого правки intro) + `tests/lime-presets.selftest.cjs`
    (все 10 секций регистрируются и материализуются, __slot-маркеры на hero/reel, publish рендерит allowlisted
    YouTube-embed, публикация не течёт __slot, scoped CSS с Playfair Display компилируется) + полный прогон
    всех node-селфтестов + dotnet 253/253 (бэкенд не тронут) — зелёные. E2E на живом стенде: оба тайла пака в
    интро, клик применяет 44 блока, портрет+рил-embed рендерятся (реальное видео), marquee едет, заголовок в
    Playfair Display, save→publish→опубликованная страница при настоящем mobile-viewport (reduced-motion) — 0px
    горизонтального переполнения, embed lazy+sandboxed, `__slot` не течёт, 0 неожиданных консольных ошибок.
- Milestone 2 — уровни UI (Basic/Design/Motion/Pro) как прогрессивное раскрытие, без новых paywall-гейтов.
  - ✅ СТАТУС 2026-07-04: ЗАКРЫТ. Обобщили уже существовавший 2-уровневый механизм (`STYLE_REGISTRY`'s `adv:true` → collapsible «Дополнительно») в полноценные 4 уровня: `tier` (`basic|design|motion|pro`) на каждом контроле вместо булева `adv`; всё, что выше текущего уровня, сворачивается в тот же «Дополнительно» (не исчезает — раскрытие обучающее, не paywall). Новый модуль `lime-editor-ui-level.js` (`window.LimeEditorUiLevel`: `rank`/`atOrBelow`/`get`/`set`/`wireToggle`) — сегментированный тумблер уровня в topbar-more меню, персист в `localStorage["lime-ui-level"]`. Дефолт уровня переиспользует существующий флаг тура (`lime-onboarding-seen`): совсем новый юзер → Basic, уже видевший редактор → Pro (=как сегодня, без сюрприза «куда делись контролы»). На Pro поведение пиксель-в-пиксель как до этого этапа (fold всегда пуст). Гейты: Style-tab секции (Layout·V2/Классы/пропсы компонента → design; Тень/Прозрачность/Blend/Мин.высота → pro; Граница/Трекинг/Регистр → design) через общий `tieredSections`; вкладки FX/Движение скрыты целиком ниже Motion; «Код сайта» + обе ссылки экспорта скрыты ниже Pro; «Превью анимаций» — ниже Motion.
  - Найден и исправлен попутный баг (не от этой правки, но проявился при тестировании гейтинга): `.lime-topbar-more__item` жёстко задавал `display:flex`, перебивая нативный `[hidden]{display:none}` — тот же класс CSS-ловушки, что уже был решён для `.lime-topbar-more__menu[hidden]` ранее, но для отдельных пунктов меню правила не было. Добавлено `.lime-topbar-more__item[hidden]{display:none}`.
  - Проверено: новый `tests/lime-ui-level.selftest.cjs` + обновлённый `tests/lime-inspector.selftest.cjs` (adv-фикстура переведена на `tier`, добавлена pro-уровня проверка) + полный прогон всех node-селфтестов — зелёные; dotnet 250/250 (бэкенд не тронут). E2E на живом стенде (Playwright, одноразовый скрипт): свежий юзер → дефолт Basic подтверждён (только вкладка «Стиль», fold есть, «Код сайта»/«Превью» скрыты) → пройдены все 4 уровня по тумблеру с ожидаемым набором вкладок/fold/видимости пунктов на каждом → reload сохраняет выбранный уровень. 1 консольная ошибка — предсуществующая (regex в `UserNamePolicy.cs`, не связана с этой правкой).
  - Вне рамок этого этапа (сознательно): фон/CMS-биндинг/embed-настройки/hover-toggle не гейтятся (интринсик для типа блока — сломало бы использование блока); содержимое FX-таба гейтится целиком, без разделения на layout-ish/motion-ish части; «advanced SEO» из текста плана не гейтится — метаполя сайта не найдены в инспекторе блока при разведке, остаются для отдельного захода; Ctrl+K команды `open-code`/`open-theme` намеренно не гейтятся (power-user bypass).
- Milestone 3 — motion/scene UX: готовые рецепты сцен + человеческие пресеты в инспекторе.
  - ✅ СТАТУС 2026-07-04: ЗАКРЫТ. Человеческие пресеты вместо голых чисел: параллакс-глубина
    (Нет/Лёгкий/Средний/Сильный), длина сцены (Коротко/Средне/Длинно/Оч. длинно) и — новое — скорость
    бегущей строки (Медленно/Обычно/Быстро, раньше вообще не была видна в UI, хардкодилась 40) стали
    сегами вместо range-слайдеров; кнопка подсвечивается по ближайшему значению, старые документы
    (в т.ч. `neo-lore-drop` со значениями вроде 0.28/0.30) не ломаются, просто визуально округляются.
    Слайдеры декор-слоёв (размер/z/глубина/блюр/прозрачность) сознательно оставлены как есть —
    свободная пространственная подстройка, не бакетируется осмысленно.
  - Готовые рецепты сцен (5 шт., один клик применяет пачку motion-настроек на уже ВЫБРАННЫЙ блок,
    в отличие от LimePresets/Experience Packs, которые вставляют новую секцию): «Закреплённый герой»
    (`scene:{pin,2}`+fade-up), «Горизонтальные карточки» (`scene:{horizontal,3}` — та же комбинация,
    что вручную применена к `neo-factions` в спайке Milestone 0), «Слоистый параллакс» (parallax+2
    декор-слоя, работает на любом блоке), «Бегущая строка» (`marquee:{30,false}`), «Последовательное
    появление» (staggered fade-up по всем прямым детям контейнера, delay = i×120мс — единственный
    рецепт, требующий батч на несколько блоков, а не один). Container-only гейтинг там, где поле
    `scene`/`marquee`/`.children` того требует (совпадает с существующими гейтами
    `sceneInspector`/`motionInspector`).
  - Механизм: `runCommands` (уже был инжектирован в effects.js, уже использовался в `setAnim` для
    батча из 3 команд) — ноль новых полей в whitelist `lime-commands.js`, всё уже разрешено.
  - Найден и исправлен реальный баг по ходу проверки (не в новом коде рецептов, а в том, как я сам
    его написал): `runCommands` обновляет только модель `doc`, но НЕ патчит холст сама — в отличие
    от `setBlockValue`/`setAnim`, которые после `runCommands` вручную обновляют DOM-атрибуты. Мой
    `applyRecipe` изначально не перерисовывал канвас в cmdStore-ветке (только `refreshInspector()`),
    из-за чего рецепт применялся к модели, но визуально холст не менялся. Исправлено: `render()`
    зовётся всегда (обе ветки — с cmdStore и без), `refreshInspector()` — тоже.
  - Проверено: новый `tests/lime-editor-effects.selftest.cjs` (21 проверка: closest-match для всех
    3 пресетов, container-гейтинг рецептов, все 5 apply-сценариев включая батч на детей, cmdStore/
    non-cmdStore ветки) + полный прогон всех node-селфтестов + dotnet 250/250 — зелёные. E2E на
    живом стенде (Playwright): применение «Горизонтальные карточки» к уже-horizontal `neo-factions`
    идемпотентно (те же `scene:{horizontal,3}`, значит рецепт даёт ровно то, что и ручная настройка);
    гейтинг подтверждён (контейнер видит все 5 тайлов, текстовый блок — только «Слоистый параллакс»);
    «Последовательное появление» корректно проставило `data-anim-delay="120"/"240"` на детей на
    холсте (у первого ребёнка delay=0 не рендерится атрибутом — pre-existing `if(block.animDelay)`
    в `lime-doc.js`, 0 falsy, функционально эквивалентно отсутствию задержки, не баг). Консольная
    ошибка — та же предсуществующая (regex в `UserNamePolicy.cs`).
  - Вне рамок этого этапа (сознательно): слайдеры декор-слоёв не тронуты; для пресетов нет
    параллельного «точного значения» контрола — упрощение, не недосмотр; новых рецептов сверх 5
    из текста плана нет.
- Milestone 4 — asset slots + требования к слотам + backend-пайплайн ассетов.
  - 🟡 СТАТУС 2026-07-04: ЗАКРЫТ КЛИЕНТСКИЙ СРЕЗ (по явному выбору пользователя — backend-пайплайн
    видео НЕ трогаем, честно ограничиваемся внешним URL). `assetSlots` из Milestone 1
    (`lime-experience-packs.js`) имели ноль потребителей UI с момента добавления — закрыт именно
    этот разрыв, не новая модель данных. Два новых связующих поля, оба additive/безопасные для
    старых документов: `doc.pack = key` штампуется в `applyTemplateByKey` (для ЛЮБОГО шаблона, не
    только Experience Pack — для обычных `resolve()` просто вернёт null); `content.__slot:
    "hero-scene"`/`"customizer-scene"` на двух embed-блоках `neo-hero`/`neo-customizer` (третий слот,
    `logo`, остаётся только метаданными — в `neo-navbar` нет image/logo-блока, чтобы его пометить, а
    заводить ради этого новый блок в уже собранной секции — scope creep, сознательно не сделано).
  - Требования к слоту показываются в инспекторе (`contentExtras` → новая секция «Требования к
    ассету», reuse существующего `.lime-inspector__hint`) и в самом промпте `promptEmbed` (подсказка
    добавляется к тексту «Ссылка на …:») — безопасный no-op для любого документа без `doc.pack`
    (все существующие/старые доки просто не видят новую секцию, ничего не сломано).
  - Честная пометка про видео: под кнопкой «+ Видео-фон» (`lime-editor-section-bg.js`) добавлена
    строка «Только внешняя ссылка на .mp4/.webm — загрузка файла пока не поддерживается» — это было
    единственное место, где кнопка сама по себе не исключала ожидание диалога загрузки.
  - Проверено: 4 существующих теста расширены (`lime-presets`/`lime-content-binding`/
    `lime-media-actions`/`lime-section-bg`.selftest.cjs — новые проверки на stamp/no-op-цепочку/
    slot-aware промпт/честную пометку) + полный прогон всех node-селфтестов + dotnet 250/250
    (бэкенд не тронут) — зелёные. E2E на живом стенде (Playwright): hero/customizer embed показывают
    каждый свой отдельный hint; обычный текстовый блок и сайт из плоского `startup`-шаблона никогда
    не видят slot-UI (doc.pack не резолвится → тихий no-op); подтверждено, что `__slot` не течёт в
    опубликованный HTML (embed-рендерер читает только явно перечисленные поля content).
  - Остаётся открытым (сознательно, по выбору пользователя): загрузка видео-файлов на бэкенд (новая
    валидация/лимиты/`IMediaStorage`), `logo`-слот без блока-носителя, avif-загрузка (опционально,
    не блокер по тексту плана).
- Milestone 5 — AI creative assistant по фазам A→D (см. Key Changes).
  - ✅ СТАТУС 2026-07-04: ФАЗА A ЗАКРЫТА — «заполнить секции пака текстом». Ноль нового бэкенд-эндпоинта
    и ноль новых типов команд/whitelist: переиспользован тот же `/Ai/Suggest` →
    `SuggestCommandsAsync` → `setContent`-команды → тот же клиентский конвейер (validate → dry-run →
    preview → один undo), что и одиночный `aiSuggest`. Отличается только клиентская сериализация:
    новый `collectTextBlocks` (`lime-editor-ai-pipeline.js`) рекурсивно собирает текстовые поля СО
    ВСЕГО документа (а не поддерева одного блока) в плоскую карту `{id, type, content:{field:value}}`,
    включая элементы массивов (testimonials/FAQ/navbar-links) через пути вида `items.0.quote` —
    `fieldPath` в `lime-commands.js` уже умел писать по таким путям, ничего не добавлено. Служебные
    строковые поля (`width`, `provider`, `embedUrl`, `poster` и т.п.) и URL-строки в exclude-списке —
    иначе AI мог бы «переписать» embed-ссылку или сломать раскладку.
  - Новая кнопка «AI: заполнить текстом» в topbar-more (рядом с «AI заново», но по смыслу другая:
    «AI заново» — с нуля, эта — сохраняя структуру/motion пака) + запись в Ctrl+K. Работает только
    когда `doc.pack` резолвится в настоящий Experience Pack через `LimeExperiencePacks.resolve()` —
    иначе дружелюбное сообщение.
  - `MaxCommands` поднят 40→100 (сервер `AiContentService.cs` + клиент): замерено скрипт-подсчётом
    перед правкой — `neo-lore-drop` даёт 71 текстовое поле после чистки от служебных, старый лимит
    был бы тесен. Обновлён xUnit-тест, жёстко зависевший от старого значения 40.
  - Найден и исправлен реальный баг при E2E-проверке: guard «только для пака» проверял голую
    truthiness `doc.pack`, а не то, резолвится ли он в НАСТОЯЩИЙ пак — Milestone 4 штампует `doc.pack`
    для ЛЮБОГО шаблона (включая обычные `startup`/`agency`/…), так что guard пропускал их всех.
    Исправлено на проверку через `LimeExperiencePacks.resolve(doc.pack)`; регрессия закрыта тестом.
  - Проверено: новый `tests/lime-editor-ai-pipeline.selftest.cjs` (20 проверок: разворачивание
    полей/массивов/exclude-правила, все guard-ветки, happy-path FormData+validateAiCommands,
    коды ошибок 429/503/5xx/сетевая) + полный прогон всех node-селфтестов + dotnet 250/250 —
    зелёные. E2E на живом стенде (без настроенного AI-провайдера, по установленной в проекте
    практике не тестировать живой LLM в e2e): кнопка на месте, клик показывает промпт брифа,
    вызов на реальном паке доходит до сервера и корректно показывает «AI не настроен» (503) —
    весь конвейер до сервера жив; на обычном шаблоне — дружелюбное сообщение без сетевого запроса.
  - ✅ СТАТУС 2026-07-04: ФАЗА B ЗАКРЫТА — «подобрать тему + motion под настроение». Разведка
    показала несостыковку в тексте плана: `LimeExperiencePacks.LIST` содержит РОВНО один пак
    (`neo-lore-drop`) — «AI выбирает pack» буквально не из чего выбирать. По явному решению
    пользователя выбор пака в этот заход НЕ вошёл (вернёмся, когда появится 2-й пак); сделана
    только theme+motion половина.
  - Единственная новая команда — `setTheme` (`doc.theme[key]=value`, документ-уровень, не блок —
    путь `["theme", key]` абсолютный от корня `doc`, `findPath` не нужен). Добавлена в
    `COMMANDS`/`AI_ALLOWED` (`lime-commands.js`) и в серверный `AllowedCommands`
    (`AiContentService.cs`) зеркально, как и раньше. Валидация в билдере: `key` из
    `accent|accent2|bg|fg|muted|font`, hex-регексп для цветов, длина/непустота для `font` —
    невалид → no-op, паттерн идентичен `setDesign`/`setStyle`.
  - Находка при разведке: `setBlockProp` (motion: anim/animDelay/animDuration/parallax/marquee/
    scene/sticky/stickyOffset) был разрешён в allowlist ещё ДО этой фазы, но ни разу не был
    описан модели в `CommandSystemPrompt` — то есть реальный LLM никогда не мог его использовать,
    хотя технически пропускался. Фаза B впервые документирует `setBlockProp` в системном промпте
    (наравне с новым `setTheme`) — это и есть настоящая часть работы, не только allowlist-запись.
  - Сознательно НЕ отдаём модели `fx`/`layers` (ни в промпте, ни в собираемом контексте):
    `layers` хранит массив декор-фигур, `setBlockProp` заменяет его ЦЕЛИКОМ — слепая генерация
    LLM тихо стёрла бы ручную расстановку слоёв. Это recipe-территория (`lime-editor-effects.js`),
    не для freeform LLM-авторства.
  - Новый клиентский код (`lime-editor-ai-pipeline.js`): `collectMotionBlocks` (рекурсивный сбор
    id/type/container-флага + только присутствующих motion-полей, без fx/layers) и
    `aiRestylePack()` — тот же `/Ai/Suggest` + `applyAiCommands` (validate → dry-run → preview →
    один undo), тот же guard на `LimeExperiencePacks.resolve(doc.pack)`, что и в Фазе A. Кнопка
    «AI: оформление» в topbar-more + запись в Ctrl+K. `describeAiChange` получил ветку для
    `setTheme`. Инструкция короткая (замерено — ~200-280 симв. с брифом) не случайно: `/Ai/Suggest`
    ограничивает `instruction` 300 символами на сервере (нашли при разведке) — вся схема
    `setTheme`/`setBlockProp` описана в статичном `CommandSystemPrompt`, а не в каждом запросе.
  - Проверено: расширены `tests/lime-commands.selftest.cjs` (setTheme — валидный/невалидный hex,
    unknown key, длина font, no-op на повторе, undo/redo, создание `theme` при отсутствии,
    присутствие в AI-allowlist) и `tests/lime-editor-ai-pipeline.selftest.cjs` (collectMotionBlocks
    флэттенинг + container-флаг + исключение fx/layers, все guard-ветки aiRestylePack, happy-path
    FormData/context, коды ошибок 429/503/5xx/сеть) + новый xUnit `TryParseCommands_KeepsSetTheme`
    + полный прогон всех node-селфтестов и dotnet — 251/251 зелёных (было 250). E2E на живом
    стенде (без настроенного AI-провайдера): кнопка на месте, промпт-диалог показывается, реальный
    запрос на `/Ai/Suggest` уходит с полным `context` (тема+motionProfile+blocks — подтверждено
    через перехват тела запроса) и корректно получает 503 «AI не настроен»; на плоском `startup`-
    шаблоне — дружелюбное сообщение без единого сетевого вызова; отмена промпта — no-op без
    сети; Ctrl+K находит запись. Консольный шум — тот же предсуществующий regex-варнинг
    `UserNamePolicy`, что и во всех прошлых вехах (не связан с этой правкой).
  - ✅ СТАТУС 2026-07-04: ФАЗА C ЗАКРЫТА — «предложить asset prompts под слоты». Категорически
    другая по форме фаза: НЕ мутирует документ вообще — ни content, ни commands, поэтому нет ни
    allowlist-изменений, ни validate/dry-run/preview/undo. По форме ближе к `Rewrite`/`EditBlock`
    (текст-в-текст-наружу), а не к `Suggest` (документ-командам). Новый эндпоинт
    `/Ai/SuggestAssetBrief` + `AiContentService.SuggestAssetBriefAsync` — своя система-роль
    («помогаешь дизайнерам подобрать промпт для ассета»), НЕ переиспользовали `Rewrite` с
    подставным текстом — его система-роль «ты редактор текстов лендингов» была бы смысловым
    подлогом для этой задачи. Общая квота `"ai"` (тот же метр, что у всех AI-действий) — новый
    метр не заводили.
  - Кнопка «✦ Промпт для ассета» встроена прямо в существующую секцию «Требования к ассету»
    (`slotHintSection`, `lime-editor-content-binding.js`, Milestone 4) — рендерится только когда
    слот уже резолвился в настоящий пак (та же цепочка, что и у хинта), поэтому отдельного guard'а
    «не пак» тут не нужно: кнопки просто нет в DOM. Без диалога-брифа (в отличие от Фаз A/B) —
    слот+пак+тема уже дают достаточно контекста для одного клика; данные идут через `data-*`
    атрибуты самой кнопки (посчитаны один раз в `slotHintSection`), а тема читается свежей из
    `doc.theme` в момент клика (могла измениться после Фазы B). Результат показывается прямо под
    кнопкой (`data-ai-asset-result`), не через `alert()` — это текст, который хочется читать/
    выделять, а не разовое сообщение.
  - Найдено при разведке (не баг, находка по ходу проектирования): выбор блока через
    `hover()+.lime-block-grip.click({force:true})` (рабочая техника для легаси V1-редактора из
    прошлых фаз) ненадёжен на дефолтном V2-канвасе — там своя модель выбора
    (`window.__LIME_SELECTION__`), из-за чего клик по грипу выделял НЕ тот блок. Инспектор/
    content-binding общие для V1/V2, так что сама фича не затронута — для live E2E использовали
    `?classic=1` (штатный fallback-флаг раскатки V2). Стоит иметь в виду для будущих E2E на
    per-block инспекторных фичах: топбар-кнопки (Фазы A/B) этой проблемы не имели, т.к. не требуют
    выбора конкретного блока.
  - Проверено: `tests/lime-content-binding.selftest.cjs` (кнопка+data-*+result-контейнер рендерятся
    только когда резолвится реальный слот, все существующие no-op-цепочки Milestone 4 не задеты),
    `tests/lime-editor-ai-pipeline.selftest.cjs` (dataset → context, live `doc.theme`, "AI думает…"
    до резолва fetch, happy-path/429/503/5xx/сеть), `tests/lime-inspector-events.selftest.cjs`
    (клик по `data-doc-ai-asset-prompt` доходит до инжектированной функции) + 2 новых xUnit-теста
    (`SuggestAssetBriefAsync` — trim/quote-strip/length-cap, `SecurityConfigurationTests`'
    size-limit assertion) — все новые + полный прогон существующих зелёные, dotnet 253/253 (было
    251). E2E на живом стенде (без настроенного AI-провайдера, `?classic=1`): кнопка рендерится с
    верным `data-slot-label`, клик доходит до `/Ai/SuggestAssetBrief` с полным JSON-контекстом
    (slotLabel+slotHint+packName+category+theme — подтверждено перехватом тела запроса), результат
    инлайн показывает «AI не настроен на сервере» (503); блок без `__slot` и документ из плоского
    `startup`-шаблона не показывают кнопку вообще. Консольный шум — тот же предсуществующий
    regex-варнинг `UserNamePolicy`.
  - ✅ СТАТУС 2026-07-04: ФАЗА D ЗАКРЫТА — `aiAdaptMobile` доведён до pack-level mobile-прохода
    для neo-секций. Добавлен компактный mobile-контекст по всему Experience Pack: viewport, pack
    metadata, responsive styles/design, scene/marquee/sticky-поля, scoped-css marker, embed metadata
    и asset-slot hints без URL/декор-массивов. Новый topbar/Command Palette вход «AI: мобайл»
    шлёт `/Ai/Suggest` с `breakpoint=mobile`; серверная поверхность НЕ расширялась — старый
    `SuggestResponsiveAsync` по-прежнему жёстко пропускает только `setStyle`/`setDesign` на mobile.
    Реальный `neo-lore-drop`: 58 mobile-blocks, context 17 047 символов, инструкция 213 символов
    (в лимитах клиента/сервера). Проверено selftest: AI pipeline + command registry.

## Перф-бюджет и доступность (design-constraints, не тесты)

Это ограничения на этапе проектирования, а не только строки в Test Plan.

- Embed грузится лениво и defer до попадания во вьюпорт; до загрузки — обязательный poster/fallback, чтобы не бить LCP.
- Целевой LCP showcase-страницы ≤ 2.5с на среднем мобильном; hero-poster виден мгновенно, тяжёлая сцена догружается после.
- `prefers-reduced-motion` — поведение по умолчанию, а не галочка: pinned/horizontal/parallax при reduced-motion деградируют в статичный читаемый контент.
- Mobile-деградация pinned/horizontal сцен — жёсткое правило: на узком экране они не должны ломать чтение или ловить jank; при сомнении — статичный fallback.
- ✅ Cache-Control для `/media/**` (public, max-age=31536000, immutable) — сделано 2026-07-03, prerequisite бюджета закрыт.

## Критерии успеха (измеримые)

- Новичок публикует приличный showcase за < 10 минут, ни разу не открыв Motion/Pro.
- Опубликованный `neo-lore-drop` проходит перф-бюджет выше на реальном мобильном.
- Продвинутый пользователь может заменить hero-embed и все asset slots своими медиа без правки кода.

## Связь с запуском и Editor V2 roadmap

- Это крупный scope-add рядом с launch-очередью `ROADMAP.md`. Обновление 2026-07-03: код-конкуренция снята — M4.1 (god-files) закрыт, оставшиеся ship-блокеры ROADMAP `Milestone 1` (бэкапы off-site / Sentry / SMTP) ждут внешних сервисов, а не кода. Обновление 2026-07-04: Milestone 0 (спайк) этого плана закрыт — gate пройден, эталон убеждает. По явному решению пользователя продолжили дальше без ожидания ROADMAP `Milestone 1` (тот по-прежнему открыт — внешние сервисы не подключены) — закрыт и Milestone 1 ЭТОГО плана (формат Experience Pack + canvas-first вход), чисто клиентский код, без бэкенда/биллинга/новых внешних зависимостей, поэтому конкуренции с launch-блокерами нет. Обновление 2026-07-04 (2): по продолжению того же явного решения закрыт и Milestone 2 (уровни UI Basic/Design/Motion/Pro) — тоже чисто клиентский код (JS/CSS/cshtml), без бэкенда/биллинга. Обновление 2026-07-04 (3): закрыт и Milestone 3 (рецепты сцен + человеческие пресеты) — снова чисто клиентский код, ноль изменений в `lime-commands.js`/бэкенде (все поля, которые трогают рецепты, уже были в whitelist). Обновление 2026-07-04 (4): по явному выбору пользователя закрыт клиентский срез Milestone 4 (asset-slot требования в инспекторе/промпте + честная пометка про видео) — backend-пайплайн видео (загрузка файлов) сознательно НЕ тронут, остаётся открытым. Обновление 2026-07-04 (5): по явному выбору пользователя (Фаза A из 4, названа явно) закрыта Фаза A Milestone 5 — «заполнить пак текстом». Формально это первое изменение бэкенда в цепочке 0–5 (`AiContentService.cs`: MaxCommands 40→100), но НЕ новая поверхность — ноль новых команд/allowlist-записей, только числовой лимит для уже разрешённого `setContent`. На тот момент Фазы B–D не начинались без отдельного явного решения по каждой — Фаза B добавляет настоящие новые типы AI-команд (реальное расширение allowlist), это не то же самое, что подъём лимита. Обновление 2026-07-04 (6): закрыта Фаза B Milestone 5 — «подобрать тему + motion под настроение», единственная новая AI-команда `setTheme` (allowlist-расширение и клиент, и сервер, зеркально). По явному решению пользователя выбор Experience Pack исключён из этой фазы (в `LimeExperiencePacks.LIST` ровно один пак — реально не из чего выбирать), вернёмся к этой части плана, когда появится 2-й пак. На тот момент Фазы C–D по-прежнему не начинались без отдельного явного решения. Обновление 2026-07-04 (7): закрыта Фаза C Milestone 5 — «предложить asset prompts под слоты». Новый бэкенд-эндпоинт `/Ai/SuggestAssetBrief`, но НЕ новая командная поверхность — эта фаза вообще не трогает документ (ни content, ни commands), поэтому нет ни allowlist-изменений, ни validate/dry-run/undo; ближе по форме к уже существующим `Rewrite`/`EditBlock`, чем к `Suggest`. Обновление 2026-07-04 (8): закрыта Фаза D Milestone 5 — pack-level mobile adapter для neo-секций через существующий Responsive-AI (`setStyle`/`setDesign` только на `mobile`), без новых allowlist-команд и без backend-изменений. Исходные Фазы A–D Milestone 5 закрыты. Обновление 2026-07-04 (9): по явному выбору пользователя (портфолио из названных в Assumptions SMB/портфолио/ecommerce) закрыт второй Experience Pack — `studio-folio`. Чисто клиентский код (3 JS-файла: `lime-templates.js`/`lime-experience-packs.js`/`lime-presets.js`), ноль изменений бэкенда/allowlist. Формат пака подтверждён pack-count-agnostic без единой правки инфраструктуры (`resolve()`/intro-тайлы уже поддерживали N паков с Milestone 1). AI pack-picking (отложенный в Фазе B) теперь МОГ БЫ иметь смысл — но само решение начинать его не принято, ждёт отдельного явного go-ahead.
- Явно зафиксировать, что здесь «до запуска» (максимум — спайк), а что «после». Не заводить вторую параллельную дорожную карту: место этого плана — рядом с `ROADMAP.md` (M-вехи запуска там; файла `EDITOR_V2_PLAN.md` в репо нет — прежняя ссылка была битой).
- Биллинг не трогаем (заморожен): никаких новых тарифных гейтов, ограничения custom code — как есть.

## Test Plan

- Unit/selftests: проверить рендер нового showcase-документа: layers, scenes, embed, mobile CSS, отсутствие editor-only hooks.
- Unit/selftests: расширить AI tests на новые разрешенные block/command типы и asset prompt output.
- Unit/selftests: ✅ embed покрыт в `lime-doc.selftest.cjs` и `lime-media-actions.selftest.cjs`: host-allowlist, provider autodetect/presets, aspect fallback, poster sanitize, fallback markup, publish без editor-only hooks, prompt validation.
- Playwright: ✅ ДОБАВЛЕНО 2026-07-04 — `tests/flows/showcase-launch.spec.ts` (`npm run test:e2e:showcase`): уникальный test-user -> пустой холст -> выбрать `neo-lore-drop` через intro tile -> заменить hero embed через реальный prompt-flow -> включить Motion mode -> mobile viewport -> save -> publish -> проверить публичный URL.
- Playwright: ✅ ДОБАВЛЕНО 2026-07-04 — `tests/flows/showcase-progressive-ui.spec.ts` (`npm run test:e2e:showcase`): Basic не перегружен, Design/Motion/Pro открывают нужные панели и topbar-действия, выбранный уровень сохраняется после reload.
- Playwright: 🟡 ЧАСТИЧНО ПОКРЫТО 2026-07-04 — `showcase-launch` проверяет mobile viewport на опубликованной странице: видимый контент, reduced-motion, embed fallback/lazy/sandbox, отсутствие горизонтального overflow. Остаётся визуальный overlap/snapshot ниже.
- Visual/a11y: добавить визуальный snapshot для showcase pack.
- Visual/a11y: прогнать `test:e2e:visual`, `test:e2e:a11y`, `test:e2e:editor-v2`.
- Production acceptance: частично покрыто showcase-gate: publish-страница загружается без JS-ошибок, embed lazy/sandbox/fallback markup присутствует, reduced-motion оставляет контент видимым, mobile не даёт горизонтальный overflow.

## Assumptions

- "Уровень ChainZoku" означает качество композиции, motion, ассетов и интерактивности, а не копирование сайта.
- Первый релиз делает одну сильную showcase-вертикаль, потом подход масштабируется на SMB, портфолио и ecommerce.
- Вход остается через холст, но с сильными стартовыми подсказками и experience panel.
- Генерация ассетов не обязана полностью заменять художника; Lime должен честно помогать брифом, слотами, промптами и загрузкой.
